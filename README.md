# 💖 Be My Valentine Web App

Интерактивное веб-приложение с анимациями, созданное для того, чтобы оригинально предложить стать чьим-то Валентином.

Проект состоит из Frontend части (Nuxt 3) с 3D-анимацией книги и Backend части (FastAPI) для регистрации ответа.


## ✨ Особенности

* **Интерактивность:** Кнопка "Нет" убегает и меняет текст, а кнопка "Да" увеличивается при каждой попытке отказа.
* **3D Анимация:** После согласия открывается 3D-книга с поздравлением.
* **Эффекты:** Салют из конфетти при успешном ответе.
* **Галерея:** Встроенная фото-галерея в стиле полароидных снимков.
* **Адаптивность:** Корректно работает как на ПК, так и на мобильных устройствах.
* **Логирование:** Бэкенд фиксирует точное время, когда было сказано "Да".

## 🛠 Технический стек

* **Frontend:** [Nuxt 3](https://nuxt.com/) (Vue.js), CSS 3D Transforms, Canvas Confetti.
* **Backend:** [FastAPI](https://fastapi.tiangolo.com/) (Python), Poetry.
* **Инфраструктура:** Docker, Docker Compose, Nginx (Reverse Proxy + SSL).
* **CI/CD:** GitHub Actions (автоматический деплой при пуше в `main`).

## 📂 Структура проекта

```bash
.
├── backend/            # FastAPI приложение
│   ├── main.py         # Логика API
│   ├── pyproject.toml  # Зависимости (Poetry)
│   └── Dockerfile
├── frontend/           # Nuxt 3 приложение
│   ├── app.vue         # Основная логика и верстка
│   ├── public/         # Фотографии и статика
│   └── Dockerfile
├──
